<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.easypay"
    android:targetSandboxVersion="1"
    >

    <!--    <uses-feature-->
    <!--        android:name="android.hardware.camera2"-->
    <!--        android:required="true"-->
    <!--        />-->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        >
        <activity android:name=".ui.card.CardActivity" />
        <activity android:name=".ui.qrcode.QrActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.history.HistoryActivity" />
        <activity android:name=".ui.wizard.WizardActivity" />
        <activity android:name=".ui.registration.SignInActivity" />
        <activity android:name=".ui.registration.SignUpActivity" />
        <activity android:name=".ui.services.bus.BusPaymentActivity" />
        <activity android:name=".ui.services.metro.MetroPaymentActivity" />
        <activity android:name=".ui.services.train.TrainPaymentActivity" />
        <activity android:name=".ui.home.MainActivity" />
    </application>

</manifest>